[project]
name = "termux-agent-browser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=16.1.1",
    "zeroconf>=0.150.0",
]
